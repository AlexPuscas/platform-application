{% import 'OroUIBundle::macros.html.twig'  as UI %}

{% if entity is defined %}

    {{ UI.clientButton({
        'dataUrl': path('bugtracking_issue_create', oro_action_params(entity)),
        'aCss': 'no-hash',
        'iCss': 'icon-bug',
        'dataId': entity.id,
        'label': 'bugtracking.issue.add_entity'|trans,
        'title': 'bugtracking.issue.entity.singular_label'|trans,
        'widget' : {
            'type' : 'dialog',
            'multiple' : true,
            'options' : {
                'alias': 'issue-dialog',
                'dialogOptions' : {
                    'title' : 'bugtracking.issue.add_entity'|trans,
                    'allowMaximize': true,
                    'allowMinimize': true,
                    'dblclick': 'maximize',
                    'maximizedHeightDecreaseBy': 'minimize-bar',
                    'width': 1000
                }
            }
        }
    }) }}

{% endif %}
